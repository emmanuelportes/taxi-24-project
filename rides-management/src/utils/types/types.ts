type coordinates = Array<number>

type location = {
    type: string,
    coordinates: [number]
}

type schedule = {
    open: number,
    close: number
}

export {
    location,
    coordinates,
    schedule
}

